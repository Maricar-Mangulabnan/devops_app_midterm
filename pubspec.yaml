name: devops_app_midterm
description: "A new Flutter project."
publish_to: "none"

version: 1.0.1+9

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_launcher_icons: ^0.14.3
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/icons/launcher_icon.png
  min_sdk_android: 21

flutter:
  uses-material-design: true
